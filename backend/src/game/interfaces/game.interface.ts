export interface GameInterface {
  // x: number;
  // y: number; //100 is the height
  // width: number;
  // height: number;
  // color: string;
  // score: number;
  //! for understanding purpose 
  id?: string; //? ? means optional, we may or may not include it
  name: string;
  description?: string;
  qty: number;
  //! for understanding purpose 
}